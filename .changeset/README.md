# Changesets

This directory is used by [Changesets](https://github.com/changesets/changesets) to manage versioning and changelogs.

When making a change that should be noted in the changelog, run:

```bash
npx changeset
```

This will prompt you to describe the change and select a semver bump level.
